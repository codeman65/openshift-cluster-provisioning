apiVersion: v1
kind: Secret
metadata:
  name: openshift-wildcard-cert
  namespace: openshift-ingress
type: kubernetes.io/tls
data:
  tls.crt: |
    {{ publickey | b64encode }}
  tls.key: |
    {{ privatekey | b64encode }}

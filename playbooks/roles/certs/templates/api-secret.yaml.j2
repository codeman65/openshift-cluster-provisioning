apiVersion: v1
kind: Secret
metadata:
  name: openshift-api-certificate
  namespace: openshift-config
type: kubernetes.io/tls
data:
  tls.crt: |
    {{ publickey | b64encode }}
  tls.key: |
    {{ privatekey | b64encode }}
